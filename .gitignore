.secrets.env
.venv
.venv/
__pycache__/
*.py[cod]
.pytest_cache/
.coverage
htmlcov/
.DS_Store
.superpowers/

# Guard, not a file that exists. A deploy script that copies this integration
# straight into a Home Assistant config directory would overwrite a
# HACS-managed install and desync the version HACS believes it installed.
# Installation is through HACS; keep it that way.
scripts/deploy.sh
